name: Tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  tests:
    name: CloudCommerce tests
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore CloudCommerce.Tests/CloudCommerce.Tests.csproj

      - name: Test
        run: dotnet test CloudCommerce.Tests/CloudCommerce.Tests.csproj --configuration Release --no-restore
