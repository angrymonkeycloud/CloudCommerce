# CloudLogistics Aramex Provider

Aramex rate calculation, shipment creation, labels, and tracking for `AngryMonkey.CloudLogistics`.

The adapter defaults to Aramex's development endpoints. Register it with `AddAramexCloudLogistics` and provide the account number, entity, PIN, country code, user name, and password.
