# CloudLogistics FedEx Provider

FedEx rates, shipment labels, tracking, and cancellation for `AngryMonkey.CloudLogistics`.

The adapter defaults to FedEx sandbox. Register it with `AddFedExCloudLogistics` and provide the project client ID, client secret, and shipping account number.
