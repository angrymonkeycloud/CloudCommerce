# CloudLogistics DHL Express Provider

DHL Express rating, shipping labels, tracking, and cancellation for `AngryMonkey.CloudLogistics`.

The adapter defaults to the MyDHL test API. Register it with `AddDhlExpressCloudLogistics` and supply an API user, password, and shipper account number.
